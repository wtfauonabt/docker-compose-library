version: "3.9"
services:
    nginx:
        extends:
            file: ${COMPOSE_BASE_PATH}/nginx.yml
            service: nginx-proxy-manager
        container_name: nginx

networks:
    env-network:
        name: ${NETWORK_NAME}
        external: true
